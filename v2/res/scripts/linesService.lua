local lineStatsHelper = require "lineStatsHelper"

local linesService = {}

function linesService.linesTableData()
    -- Get List of all passenger lines

    -- Line name, loaded/capacity, waiting, demand, avg segment speed, max waiting, max missed train, avg wait

end

return linesService